import React from 'react';
import PropTypes from 'prop-types';

const UserProfile = (props) => {
	return (
		<div>
			Hey There!
			<br />
			<br />I am the user profile. I will show you tabs and such for navigating
			user history.
		</div>
	);
};

UserProfile.propTypes = {};

export default UserProfile;
