import React from 'react';
import PropTypes from 'prop-types';

const PropertyCard = (props) => {
	return (
		<div>
			I show the elements of the property PropertCard
			<br />
			<br />
			And i'm good at it.
		</div>
	);
};

PropertyCard.propTypes = {};

export default PropertyCard;
